cd ~ (atalho para a pasta do usuario)

ls (lista todas as pastas disponiveis)

cd documentos(onde se encontra o projeto) / physical(nome da pasta de onde o jogo estiver)  (entra na pasta)

git add . (preparar os arquivos para o commit)

git pull https://github.com/LLIMMA/-physical2d-novotec.git (entra no repositorio do arquivo)

git status (verificar se n occoreu nenhum erro)

git clone (clona um arquivo para utilizar em outro computador)

git commit -m "comentario" (realiza o commit é como se tirasse uma foto e salvasse)

git push (envia o arquivo pra nuvem)



